# Normalize line endings: store text as LF in the repo, check out native.
* text=auto eol=lf

# Treat known binary assets as binary (never normalize).
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
